# NBA API - Análise de Regressão

Este repositório contém o projeto da biblioteca `nba_api` e também uma atividade acadêmica sobre análise de regressão.

## 📁 Onde está a atividade?

A atividade de **Análise de Regressão NBA** está localizada na pasta `Atividade_nba/`. É uma aplicação web desenvolvida com Streamlit que permite analisar dados de jogadores e times da NBA usando modelos de regressão linear e logística.

## 🚀 Como rodar a atividade?

É bem simples! Siga os passos abaixo:

### 1. Entre na pasta da atividade
```bash
cd Atividade_nba
```

### 2. Instale as dependências
```bash
pip install -r requirements.txt
```

### 3. Execute a aplicação
```bash
streamlit run app.py
```

Pronto! O Streamlit vai abrir automaticamente no seu navegador. Se não abrir, acesse `http://localhost:8501`.

## 📚 O que a aplicação faz?

A aplicação permite:
- Analisar dados de jogadores e times do Golden State Warriors
- Criar features (características) a partir dos dados dos jogos
- Treinar modelos de regressão linear e logística
- Visualizar resultados com gráficos e métricas

## 💡 Dicas

- Certifique-se de ter Python 3.7+ instalado
- Se você tiver problemas com dependências, pode criar um ambiente virtual:
  ```bash
  python -m venv venv
  # Windows
  venv\Scripts\activate
  # Linux/Mac
  source venv/bin/activate
  ```

---

**Nota**: Este repositório também contém o código-fonte da biblioteca `nba_api` (um projeto open-source). A atividade acadêmica está especificamente na pasta `Atividade_nba/`.
